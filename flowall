# Run flowjs and format output for passing into Vim's Quickfix: `vim -q <(. $dotfiles/flowall)`
npm run flow check 2>/dev/null | awk 'BEGIN { details = -1 } $1 == "Error" { position = $3; details = 2 } details == 0 {print position, $0} {details--}'
