# -*- mode: snippet -*-
# name: react-template
# key: react-template
# --
// @flow

import React from "react";
import { useObserver } from "mobx-react-lite";

type $1Args = {
     $3
};

export default function $1($2) {

  return useObserver(() => {
    return(
      $0
    );
  });
}